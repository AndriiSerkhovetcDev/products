import { DuplicateSearchDirective } from './duplicate-search.directive';

describe('DuplicateSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new DuplicateSearchDirective();
    expect(directive).toBeTruthy();
  });
});
